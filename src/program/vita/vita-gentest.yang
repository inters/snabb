module vita-gentest {
  namespace vita:gentest;
  prefix gentest;

  grouping interface {
    leaf pci { type string; }
    leaf ip4 { type string; }
    leaf mac { type string; }
    leaf nexthop-ip4 { type string; }
    leaf nexthop-mac { type string; }
  }

  container private-interface { uses interface; }
  container public-interface { uses interface; }

  leaf route-prefix { type string; }
  leaf nroutes { type uint32; }
  leaf packet-size { type string; }
}
